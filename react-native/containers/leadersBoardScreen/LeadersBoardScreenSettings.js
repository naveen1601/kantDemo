import $primary from '../../settings/styles/DefaultPrimarySettings';

let $_ = {
    // screenBackgroundColor: $primary.primaryColor,
    // errorMessageColor: $primary.informationColors.error,
    // validationErrorFontSize: $primary.baseFontSize,
    // validationErrorMarginHorizontal: $primary.baseGridUnit,
    // validationErrorMarginTop: $primary.baseGridUnit / 4,
    // validationErrorMarginBottom: $primary.baseGridUnit / 4,
    // labelTextFontSize: $primary.baseFontSize * 2,
    // gradeBoxMarginTop: $primary.baseGridUnit,
    // gradeBoxMarginBottom: $primary.baseGridUnit * 3,
    // nameBoxMarginTop: $primary.baseGridUnit * 3,
    // nameBoxMarginBottom: $primary.baseGridUnit * 3,
    // nameTextPaddingLeft: $primary.baseGridUnit,
    // nameTextPaddingBottom: $primary.baseGridUnit * 2,
}

export default $_;