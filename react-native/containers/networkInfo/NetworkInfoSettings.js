import $primary from '../../settings/styles/DefaultPrimarySettings';

let $_ = {
    offlineTextColor: $primary.white,
    offlineBackgroundColor: $primary.informationColors.error
};

export default $_;
